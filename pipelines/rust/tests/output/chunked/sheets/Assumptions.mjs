// sheets/Assumptions.mjs — AUTO-GENERATED by rust-parser (chunked mode)
// Do not edit manually — re-run the pipeline to regenerate.

export const SHEET_NAME = "Assumptions";
export const SHEET_DEPENDENCIES = [];

import { _index, _match, _vlookup, _hlookup, _large, _small, _rank, _fn, _sumif, _sumifs, _countif, _countifs, _offset, _matchesCriteria, _colNum, _numToCol, computeNPV, computeIRR, computeXIRR, computePMT, computePV, computeFV, computeRATE, computeNPER } from './_helpers.mjs';

/**
 * Compute all cells for sheet "Assumptions".
 * @param {{Object}} ctx - Context with get(addr), set(addr, val), values map
 */
export function compute(ctx) {
  // ── Literal / input cells ──
  ctx.set("Assumptions!A1", `Parameter`);
  ctx.set("Assumptions!B1", `Value`);
  ctx.set("Assumptions!A2", `Revenue`);
  ctx.set("Assumptions!B2", 1000000);
  ctx.set("Assumptions!A3", `COGS`);
  ctx.set("Assumptions!B3", 400000);
  ctx.set("Assumptions!A4", `OpEx`);
  ctx.set("Assumptions!B4", 150000);
  ctx.set("Assumptions!A5", `InterestRate`);
  ctx.set("Assumptions!B5", 0.06);
  ctx.set("Assumptions!A6", `InitialDebt`);
  ctx.set("Assumptions!B6", 500000);
  ctx.set("Assumptions!A7", `TaxRate`);
  ctx.set("Assumptions!B7", 0.25);
  ctx.set("Assumptions!A8", `ExitMultiple`);
  ctx.set("Assumptions!B8", 12);
  ctx.set("Assumptions!A9", `RepayRate`);
  ctx.set("Assumptions!B9", 0.2);
  ctx.set("Assumptions!A10", `YearsToExit`);
  ctx.set("Assumptions!B10", 5);

}
