// sheets/Summary.mjs — AUTO-GENERATED by rust-parser (chunked mode)
// Do not edit manually — re-run the pipeline to regenerate.

export const SHEET_NAME = "Summary";
export const SHEET_DEPENDENCIES = ["Cashflows"];

import { _index, _match, _vlookup, _hlookup, _large, _small, _rank, _fn, _sumif, _sumifs, _countif, _countifs, _offset, _matchesCriteria, _colNum, _numToCol, computeNPV, computeIRR, computeXIRR, computePMT, computePV, computeFV, computeRATE, computeNPER } from './_helpers.mjs';

/**
 * Compute all cells for sheet "Summary".
 * @param {{Object}} ctx - Context with get(addr), set(addr, val), values map
 */
export function compute(ctx) {
  // ── Literal / input cells ──
  ctx.set("Summary!A1", `KPI`);
  ctx.set("Summary!B1", `Value`);
  ctx.set("Summary!A2", `Revenue`);
  ctx.set("Summary!A3", `EBITDA`);
  ctx.set("Summary!A4", `CashFlowPostDebt`);
  ctx.set("Summary!A5", `ExitValue`);
  ctx.set("Summary!A6", `ROE`);
  ctx.set("Summary!A7", `MinKPI`);
  ctx.set("Summary!A8", `MaxKPI`);
  ctx.set("Summary!A9", `SumKey`);
  ctx.set("Summary!A10", `RatingText`);
  ctx.set("Summary!A11", `NetMargin`);
  ctx.set("Summary!A12", `LeverageRatio`);

  // ── Formula cells ──
  ctx.set("Summary!B2", ctx.get("Cashflows!B2"));
  ctx.set("Summary!B3", ctx.get("Cashflows!B5"));
  ctx.set("Summary!B4", ctx.get("Cashflows!B7"));
  ctx.set("Summary!B5", ctx.get("Cashflows!B15"));
  ctx.set("Summary!B6", ctx.get("Cashflows!B16"));
  ctx.set("Summary!B7", Math.min(...[ctx.get("Cashflows!B2"),ctx.get("Cashflows!B5"),ctx.get("Cashflows!B13")].flat()));
  ctx.set("Summary!B8", Math.max(...[ctx.get("Cashflows!B2"),ctx.get("Cashflows!B5"),ctx.get("Cashflows!B13")].flat()));
  ctx.set("Summary!B9", [ctx.get("Cashflows!B2"),ctx.get("Cashflows!B5"),ctx.get("Cashflows!B7")].flat().reduce((a,b)=>a+(+b||0),0));
  ctx.set("Summary!B10", (((ctx.get("Cashflows!B16") > 1)) ? (`Strong`) : ((((ctx.get("Cashflows!B16") > 0)) ? (`Moderate`) : (`Weak`)))));
  ctx.set("Summary!B11", (((ctx.get("Cashflows!B5") > 0)) ? ((ctx.get("Cashflows!B13") / ctx.get("Cashflows!B5"))) : (0)));
  ctx.set("Summary!B12", (((ctx.get("Cashflows!B8") > 0)) ? ((ctx.get("Cashflows!B9") / ctx.get("Cashflows!B8"))) : (0)));
}
